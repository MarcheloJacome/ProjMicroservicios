
server:
  port: 8081

spring:
  cloud:
    config:
      service:
      git:
        url: https://github.com/MarcheloJacome/ProjMicroservicios.git
        searchPaths: config-data
        username: ${GIT_USER}
        password: ${GIT_PASSWORD}